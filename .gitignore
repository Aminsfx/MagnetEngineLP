# Environment variables — never commit secrets
.env
.env.local
.env.production

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase CLI local state — infra identifiers, not meant for version control
supabase/.temp
supabase/.branches

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
magnetengine-extension.zip

# Perf harness output (src/perf/perf.budgets.test.tsx)
perf-report.txt

# Claude Code agent worktrees — live git worktrees, never version-controlled
.claude/worktrees/

# Business strategy / GTM planning — local working notes, not part of the product
HORMOZI-BRIEF.md
ROAD-TO-100-USERS.md
